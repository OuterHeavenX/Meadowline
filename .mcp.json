{
  "mcpServers": {
    "fal-ai": {
      "command": "npx",
      "args": [
        "-y",
        "mcp-remote",
        "https://mcp.fal.ai/mcp",
        "--header",
        "Authorization: Bearer ${FAL_KEY}"
      ]
    }
  }
}
